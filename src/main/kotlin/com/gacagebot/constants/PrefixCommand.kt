package com.gacagebot.constants

enum class PrefixCommand(val command: String) {
    PREFIX("g"),
    CAGE("cage"),
    JOIN("join"),
    PLAY("play"),
    STOP("stop"),
    SKIP("skip"),
    PAUSE("pause"),
    RESUME("resume"),
    LEAVE("leave");
}