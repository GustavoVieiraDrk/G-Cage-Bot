package com.gacagebot.constants

enum class PrefixCommand(val command: String) {
    PREFIX("g"),
    JOIN("join"),
    PLAY("play"),
    STOP("stop"),
    SKIP("skip"),
    PAUSE("pause"),
    RESUME("resume"),
    LEAVE("leave");
}